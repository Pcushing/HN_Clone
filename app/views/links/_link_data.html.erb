<%= link.votes_count %> points
| By: <%= link.user.email %>
| <%= ((Time.now - link.created_at)/3600).to_i %> hours ago    <!-- extract into helper method -->
| <%= link_to "#{ get_responses_count(link) } comments", link_path(link.id)  %>

