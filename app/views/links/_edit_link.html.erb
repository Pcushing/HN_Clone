<% if signed_in? && current_user.id == link.user_id && less_than_fifteen_minutes_from_creation?(link) %>
  <%= link_to "Edit link", edit_link_path(link.id), :class => "hover_hidden pull-right" %>
<% end %>